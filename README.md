# UserLoginSystemApplication
I had an issue with implementing JWT and swagger.
- Spring-Boot version is 3 which uses jakarta namespace instead of javax.
- But Swagger still using old version namespace (javax).
